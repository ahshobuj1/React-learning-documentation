import Raect from 'react';
import Form from './components/form';

function App() {
    const handleValue = () => {};

    return (
        <div>
            <Form getValue={handleValue} />
        </div>
    );
}

export default App;
